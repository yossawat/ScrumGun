ext.config = [
        compileSdkVersion: 32,
        applicationId    : "per.scrumgun",
        minSdkVersion    : 26,
        targetSdkVersion : 32,
        buildToolsVersion: "30.0.3"
]

def config = ext.config
